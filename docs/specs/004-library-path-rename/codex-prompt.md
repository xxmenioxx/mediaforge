# Codex — Library Path Rename

Implement `docs/specs/004-library-path-rename/`.

The reuse boundary has already been identified in `design.md`.

Start with Task 1 only:
extract the smallest read-only planner boundary from the existing direct-publication naming flow, keep current behavior unchanged, add focused regressions, then stop.
